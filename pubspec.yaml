name: intercom_demo
description: Technical demo for Azail Security System - Smart Intercom Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1
  
  name: intercom_demo
  description: Technical demo for Azail Security System - Smart Intercom Application
  publish_to: 'none'
  version: 1.0.0+1

  environment:
    sdk: '>=3.0.0 <4.0.0'

  dependencies:
    flutter:
      sdk: flutter
  
    # State Management
    provider: ^6.1.1
  
    # GraphQL Client
    graphql_flutter: ^5.1.2
  
    # Video Player (RTSP support)
    flutter_vlc_player: ^7.4.0
  
    # HTTP requests
    http: ^1.1.0
  
    # Local Storage
    shared_preferences: ^2.2.2
  
    # UI Components
    cupertino_icons: ^1.0.6


  dev_dependencies:
    flutter_test:
      sdk: flutter
    flutter_lints: ^3.0.0

  flutter:
    uses-material-design: true
  
    assets:
      - assets/images/
